---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: upgradeorchestrators.apps.nachoperator.io
spec:
  group: apps.nachoperator.io
  names:
    kind: UpgradeOrchestrator
    listKind: UpgradeOrchestratorList
    plural: upgradeorchestrators
    singular: upgradeorchestrator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The current state of the upgrade orchestrator.
      jsonPath: .status.state
      name: State
      type: string
    - description: The target Kubernetes version for the upgrade.
      jsonPath: .status.targetVersion
      name: Target Version
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpgradeOrchestrator is the Schema for the UpgradeOrchestrator
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UpgradeOrchestratorSpec defines the desired state of UpgradeOrchestrator.
            properties:
              deployments:
                description: Deployment plan with dependencies between components.
                items:
                  description: DeploymentPlan define un solo componente a ser gestionado
                    por el orquestador.
                  properties:
                    dependsOn:
                      description: List of other deployments/statefulsets that must
                        be completed before this one.
                      items:
                        type: string
                      type: array
                    name:
                      description: Name of the Deployment or StatefulSet to manage.
                      minLength: 1
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - deployments
            type: object
          status:
            description: UpgradeOrchestratorStatus defines the observed state of UpgradeOrchestrator.
            properties:
              lastTransitionTime:
                description: LastTransitionTime registra cuándo ocurrió la última
                  transición de estado. Útil para timeouts.
                format: date-time
                type: string
              state:
                description: |-
                  State indica el estado global del orquestador.
                  "NoUpgrade": Estado normal y estable.
                  "AwaitingUpgradeConfirmation": Se ha detectado un cordon, esperando confirmación de upgrade.
                  "UpgradeInProgress": Upgrade confirmado y en progreso.
                enum:
                - NoUpgrade
                - AwaitingUpgradeConfirmation
                - UpgradeInProgress
                type: string
              targetVersion:
                description: |-
                  TargetVersion es la versión de Kubernetes a la que se está actualizando el clúster.
                  Solo se rellena cuando el estado es "UpgradeInProgress".
                type: string
              workloadStatuses:
                description: WorkloadStatuses contiene el estado detallado de cada
                  workload gestionado.
                items:
                  description: WorkloadStatus representa el estado de un único workload
                    durante el upgrade.
                  properties:
                    message:
                      description: Message proporciona información adicional sobre
                        el estado, como errores.
                      type: string
                    name:
                      description: Name of the Deployment or StatefulSet.
                      type: string
                    status:
                      description: |-
                        Status representa el estado actual del workload en el proceso de upgrade.
                        Posibles valores: PendingDependencies, ReadyToMigrate, Migrating, Completed.
                      enum:
                      - PendingDependencies
                      - ReadyToMigrate
                      - Migrating
                      - Completed
                      type: string
                  required:
                  - name
                  - status
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
